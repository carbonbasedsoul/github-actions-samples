apiVersion: v1
kind: Service
metadata:
  name: hello-gitops
  labels:
    app: hello-gitops
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 8050
      targetPort: http
  selector:
    app: hello-gitops
    version: blue # initial version - will be switched by workflow
